import React from "react";
export type LabelContentProps = {
    label?: string;
    children?: React.ReactNode | string;
};
export declare const LabelContent: ({ label, children, }: LabelContentProps) => React.ReactNode;
